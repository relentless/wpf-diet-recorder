<Window x:Class="DietRecorder.Client.DetailsList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Diet Recorder" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" Name="MeasurementList" ItemsSource="{Binding}"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Name"/>
                <TextBox Name="Name"/>
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock Text="Date"/>
                <TextBox Name="Date"/>
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="Weight Kg"/>
                <TextBox Name="Weight"/>
            </StackPanel>
            <Button Grid.Column="3" Content="+" Name="AddButton" Click="AddButton_Click"/>
        </Grid>

    </Grid>
</Window>
